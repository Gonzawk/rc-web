import { AlertTriangle,Banknote,BarChart3,PackagePlus,ReceiptText,RotateCcw,ShoppingCart,Warehouse } from 'lucide-react'
import { Link } from 'react-router-dom'
import { useAppData } from '../../context/AppDataContext'
import { money } from '../../utils/commerce'
import { cashSummary } from '../../utils/cash'
export default function AdminDashboardPage(){const{data,loading,resetDemo}=useAppData();if(loading)return <div className="screen-loader">Cargando panel…</div>;const sales=data.sales||[];const revenue=sales.reduce((s,x)=>s+x.total,0);const gross=sales.reduce((s,x)=>s+x.grossProfit,0);const costValue=data.products.reduce((s,p)=>s+p.stock*(p.costPrice||0),0);const low=data.products.filter(p=>p.stock<=p.minStock).length;const cash=cashSummary(data.cashMovements||[]);const stats=[['Pedidos pendientes',(data.orders||[]).filter(x=>x.status==='pending').length,ShoppingCart],['Ventas registradas',sales.length,ReceiptText],['Facturación histórica',money(revenue),ReceiptText],['Ganancia bruta',money(gross),PackagePlus],['Capital en stock',money(costValue),Warehouse],['Saldo de caja',money(cash.balance),Banknote]];return <><div className="admin-page-header"><div><span className="section-kicker">Panel de la repuestera</span><h1>Resumen del negocio</h1><p>Compras, costos, stock, pedidos online y ventas presenciales dentro del mismo circuito.</p></div><button className="btn btn-secondary" onClick={resetDemo}><RotateCcw size={17}/> Restaurar datos</button></div><div className="stats-grid business-stats">{stats.map(([label,value,Icon])=><article className="stat-card" key={label}><Icon size={20}/><strong>{value}</strong><span>{label}</span></article>)}</div>{low>0&&<div className="dashboard-warning"><AlertTriangle size={19}/><div><strong>{low} productos requieren reposición</strong><span>Revisá stock mínimo antes de confirmar pedidos de WhatsApp.</span></div><Link to="/admin/inventario">Ver inventario</Link></div>}<div className="admin-info-grid"><Link className="admin-panel action-panel" to="/admin/compras"><PackagePlus size={22}/><h2>Registrar compra / carga</h2><p>Ingresa stock, actualiza costos y recalcula precios automáticos.</p></Link><Link className="admin-panel action-panel" to="/admin/ventas/nueva"><ReceiptText size={22}/><h2>Nueva venta presencial</h2><p>POS rápido que valida existencias y descuenta stock al confirmar.</p></Link><Link className="admin-panel action-panel" to="/admin/pedidos"><ShoppingCart size={22}/><h2>Pedidos de WhatsApp</h2><p>Editá productos, cantidades y precios antes de convertir el pedido en venta.</p></Link><Link className="admin-panel action-panel" to="/admin/inventario"><Warehouse size={22}/><h2>Stock y movimientos</h2><p>Auditoría de cada compra, venta y ajuste manual.</p></Link><Link className="admin-panel action-panel" to="/admin/caja"><Banknote size={22}/><h2>Caja general</h2><p>Ingresos por ventas, egresos por compras y movimientos manuales.</p></Link><Link className="admin-panel action-panel" to="/admin/estadisticas"><BarChart3 size={22}/><h2>Estadísticas</h2><p>Productos más rentables, rotación, cobertura y reposición.</p></Link></div><div className="architecture-note"><strong>Regla crítica de stock</strong><p>El carrito público no reserva ni descuenta unidades. Solo una venta confirmada o una compra confirmada modifica inventario. En producción, ASP.NET Core + PostgreSQL será la autoridad de estas transacciones.</p></div></>}
